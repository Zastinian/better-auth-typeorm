{
  "name": "test",
  "private": true,
  "scripts": {
    "test": "pnpm run generate && cross-env NODE_OPTIONS=\"--import tsx\" vitest run index.test.ts && cross-env NODE_OPTIONS=\"--import tsx\" vitest run sqlite.test.ts",
    "generate": "rimraf typeorm && pnpm exec auth generate --config sqlite.ts -y"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "auth": "^1.6.11",
    "cross-env": "^7.0.3",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.0"
  },
  "peerDependencies": {
    "typescript": "^6"
  },
  "dependencies": {
    "@better-auth/test-utils": "^1.6.11",
    "better-auth": "^1.6.11",
    "typeorm": "^1.0.0",
    "vitest": "^4.1.7",
    "better-sqlite3": "^12.10.0"
  },
  "pnpm": {
    "overrides": {
      "better-auth": "^1.6.11"
    }
  }
}
