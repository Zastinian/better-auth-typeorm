{
  "name": "test",
  "private": true,
  "scripts": {
    "test": "bun run generate && bunx --bun vitest run index.test.ts && bun test sqlite.test.ts",
    "generate": "rm -rf typeorm && bunx auth generate --config sqlite.ts -y"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  },
  "peerDependencies": {
    "typescript": "^6"
  },
  "dependencies": {
    "@better-auth/test-utils": "^1.5.6",
    "better-auth": "^1.5.6",
    "sqlite3": "^5.1.7",
    "typeorm": "^0.3.27",
    "vitest": "^4.1.1"
  },
  "resolutions": {
    "better-auth": "^1.5.6"
  },
  "overrides": {
    "better-sqlite3": "0.0.0"
  }
}
