{
  "name": "test",
  "private": true,
  "scripts": {
    "test": "bun test",
    "generate": "rm -rf typeorm && bunx auth generate --config sqlite.ts -y"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "better-auth": "^1.5.0",
    "sqlite3": "^5.1.7",
    "typeorm": "^0.3.27"
  },
  "resolutions": {
    "better-auth": "^1.4.3"
  },
  "overrides": {
    "better-sqlite3": "0.0.0"
  }
}
