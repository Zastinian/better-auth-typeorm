{
  "devDependencies": {
    "@biomejs/biome": "2.4.10",
    "@types/bun": "^1.3.13"
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "dev": "bun run --filter '*' dev"
  },
  "workspaces": [
    "package",
    "test"
  ]
}
